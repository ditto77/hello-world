#map
#修改大小写，将首字母大写，其余字母小写
def nor(name):
return name[0].upper()+name[1:].lower()
r=map(nor,['aba','AAAB'])
list(r)
#reduce
#编写函数，使得接受一个list，并用reduce对其求积
from fuctools import reduce
def prod (x,y):
 return x*y
 reduce(prod,[3,5,7,9])
 #reduce和map
 #编写一个函数，使得字符串‘123.45’变为浮点数123.45
